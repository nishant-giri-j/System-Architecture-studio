Created At: 2026-08-27T04:10:17+05:30
Completed At: 2026-08-27T04:10:18+05:30

The command exited with code 0.
Output:
import { Network, Save, Search, Trash, X } from 'lucide-react';
import { useState } from 'react';
import type { EventEdgeData, EventFlowEdge } from './event-edge';

interface EdgePropertiesPanelProps {
    edge: EventFlowEdge;
    onSave: (id: string, data: Partial<EventEdgeData>) => void;
    onDelete: (id: string) => void;
    onClose: () => void;
}

export function EdgePropertiesPanel({
    edge,
    onSave,
    onDelete,
    onClose,
}: EdgePropertiesPanelProps) {
    const [eventName, setEventName] = useState(edge.data?.event || '');
    const [protocol, setProtocol] = useState(edge.data?.protocol || 'HTTP');
    const [edgeStyle, setEdgeStyle] = useState(
        edge.data?.edgeStyle || 'bezier',
    );
    const [isProtocolOpen, setIsProtocolOpen] = useState(false);
    const [protocolSearch, setProtocolSearch] = useState('');

    const protocolGroups = [
        {
            label: 'Synchronous (Web/API)',
            options: [
                'HTTP',
                'HTTP/2',
                'HTTP/3',
                'REST',
                'GraphQL',
                'gRPC',
                'tRPC',
                'SOAP',
                'XML-RPC',
                'JSON-RPC',
                'Thrift',
                'OData',
                'Avro',
                'OpenAPI',
                'JSON:API',
                'gRPC-Web',
                'Connect RPC',
                'OAuth 2.0',
                'OIDC',
                'WebAuthn',
                'SOAP 1.2',

